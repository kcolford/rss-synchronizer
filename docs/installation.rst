.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install rss-synchronizer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rss-synchronizer
    $ pip install rss-synchronizer
